# 撤销和重做功能完整说明

## 📋 功能概述

系统使用 Qt 的 `QUndoStack` 实现了完整的撤销/重做功能，支持保存前和保存后的操作撤销。

---

## 🔧 撤销栈配置

### 基本设置
- **撤销栈类型**：`QUndoStack`
- **撤销步数限制**：最多支持 **50 步**撤销
- **初始化位置**：`src/ui/myform.cpp` 第142-143行

```cpp
m_undoStack = new QUndoStack(this);
m_undoStack->setUndoLimit(50);  // 限制最多50步撤销
```

### 操作方式
- **撤销**：`Ctrl+Z` 或点击工具栏撤销按钮（左箭头图标）
- **重做**：`Ctrl+Y` 或点击工具栏重做按钮（右箭头图标）

---

## ✅ 支持撤销/重做的操作

| 操作类型 | 命令类 | 实现状态 | 说明 |
|---------|--------|---------|------|
| **绘制管线** | `AddEntityCommand` | ✅ 已实现 | 绘制完成后可撤销 |
| **绘制设施** | `AddEntityCommand` | ✅ 已实现 | 绘制完成后可撤销 |
| **删除实体** | `DeleteEntityCommand` | ✅ 已实现 | 删除后可撤销恢复 |
| **粘贴样式** | `ChangeStyleCommand` | ✅ 已实现 | 样式修改后可撤销 |
| **移动实体** | `MoveEntityCommand` | ✅ 已实现 | 仅设施支持移动 |
| **修改属性** | `ChangePropertyCommand` | ✅ 已实现 | 属性修改后可撤销 |

---

## 💾 保存前的撤销/重做机制

### 核心概念

系统维护两个独立但关联的数据结构：
1. **撤销栈** (`m_undoStack`)：记录所有操作历史，用于撤销/重做
2. **待保存变更列表** (`m_pendingChanges`)：记录需要保存到数据库的变更

### 撤销操作的处理流程

当用户执行撤销操作时：

#### 1. 撤销未保存的实体（ChangeAdded）

**场景**：用户绘制了一个新管线，但还未保存

**撤销时**：
- 撤销栈执行 `undo()`，从场景中移除图形项
- `handleUndoForPendingChanges()` 检测到图形项不在场景中
- 从 `m_pendingChanges` 中移除对应的 `ChangeAdded` 记录
- **结果**：实体被删除，且不会保存到数据库

**代码位置**：`src/ui/myform.cpp::handleUndoForPendingChanges()` 第2110-2137行

```cpp
// 检查待保存变更列表中的 ChangeAdded 记录
// 如果图形项不在场景中，说明被撤销了，需要从待保存列表中移除
if (change.type == ChangeAdded && change.graphicsItem) {
    if (!sceneItemSet.contains(change.graphicsItem)) {
        // 从待保存列表中移除
        m_pendingChanges.removeAt(i);
    }
}
```

#### 2. 撤销已保存的实体（已保存到数据库）

**场景**：用户删除了一个已保存到数据库的实体

**撤销时**：
- 撤销栈执行 `undo()`，将实体重新添加到场景
- `handleUndoForPendingChanges()` 检测到实体有数据库ID（`pipeline.id() > 0`）
- 在 `m_pendingChanges` 中添加 `ChangeDeleted` 记录
- **结果**：实体重新显示，但会在下次保存时从数据库删除

**代码位置**：`src/ui/myform.cpp::handleUndoForPendingChanges()` 第2139-2188行

```cpp
// 如果不在待保存列表中，且数据库ID存在，说明已经保存到数据库
// 需要添加 ChangeDeleted 记录
if (!foundInPending && pipeline.id() > 0) {
    PendingChange change;
    change.type = ChangeDeleted;
    change.entityType = "pipeline";
    change.data = QVariant::fromValue(pipeline);
    change.originalId = pipeline.id();
    m_pendingChanges.append(change);
}
```

### 重做操作的处理流程

当用户执行重做操作时：

#### 1. 重做未保存的实体

**场景**：用户撤销了刚绘制的实体，现在要重做

**重做时**：
- 撤销栈执行 `redo()`，将实体重新添加到场景
- `handleRedoForPendingChanges()` 检测到实体在场景中，但不在待保存列表中
- 检查实体的数据库ID，如果为0或不存在，说明是未保存的实体
- 在 `m_pendingChanges` 中添加 `ChangeAdded` 记录
- **结果**：实体重新显示，且会保存到数据库

**代码位置**：`src/ui/myform.cpp::handleRedoForPendingChanges()` 第2226-2287行

```cpp
// 如果不在待保存列表中，且数据库ID不存在或为0，说明是未保存的实体被重做了
// 需要添加 ChangeAdded 记录
if (!foundInPending && pipeline.id() <= 0) {
    PendingChange change;
    change.type = ChangeAdded;
    change.entityType = "pipeline";
    change.data = QVariant::fromValue(pipeline);
    change.graphicsItem = item;
    m_pendingChanges.append(change);
}
```

#### 2. 重做已保存的实体

**场景**：用户撤销了删除操作（删除已保存的实体），现在要重做删除

**重做时**：
- 撤销栈执行 `redo()`，从场景中移除实体
- `handleRedoForPendingChanges()` 检测到场景中有 `ChangeDeleted` 记录
- 从 `m_pendingChanges` 中移除对应的 `ChangeDeleted` 记录
- **结果**：实体被删除，且不会从数据库删除（因为撤销了删除操作）

**代码位置**：`src/ui/myform.cpp::handleRedoForPendingChanges()` 第2261-2285行

```cpp
// 如果图形项在场景中，且有 ChangeDeleted 记录，说明是已保存的实体被重做了
// 需要移除 ChangeDeleted 记录
if (change.type == ChangeDeleted && change.entityType == "pipeline") {
    m_pendingChanges.removeAt(pendingIndex);
}
```

---

## 💾 保存后的撤销/重做机制

### 保存操作

**代码位置**：`src/ui/myform.cpp::onSaveAll()` 第7275-7301行

保存操作会：
1. 将所有 `m_pendingChanges` 中的变更保存到数据库
2. 调用 `clearPendingChanges()` 清空待保存变更列表
3. **重要**：**不会清空撤销栈** (`m_undoStack`)

```cpp
void MyForm::onSaveAll()
{
    if (savePendingChanges()) {
        clearPendingChanges();  // 清空待保存变更列表
        // 注意：这里没有清空撤销栈
    }
}
```

### 保存后撤销的特点

#### ✅ 可以撤销保存前的操作

**场景**：
1. 用户绘制了管线A（未保存）
2. 用户绘制了管线B（未保存）
3. 用户保存了所有变更（A和B都保存到数据库）
4. 用户撤销操作

**结果**：
- 可以撤销到保存前的状态
- 撤销后，被撤销的实体仍然在数据库中（因为已经保存）
- 撤销操作会在 `m_pendingChanges` 中添加 `ChangeDeleted` 记录
- 如果再次保存，会从数据库删除这些实体

#### ⚠️ 注意事项

1. **撤销栈不会在保存后清空**
   - 保存后仍然可以撤销保存前的所有操作
   - 撤销栈最多保留50步历史

2. **撤销已保存的实体会创建删除记录**
   - 撤销已保存的实体时，会在 `m_pendingChanges` 中添加 `ChangeDeleted` 记录
   - 下次保存时，这些实体会从数据库删除

3. **重做已撤销的已保存实体**
   - 重做后，会从 `m_pendingChanges` 中移除 `ChangeDeleted` 记录
   - 实体重新显示，且不会从数据库删除

---

## 🔄 完整工作流程示例

### 示例1：绘制新实体并保存

```
1. 用户绘制管线A
   → 撤销栈：添加 AddEntityCommand
   → 待保存列表：添加 ChangeAdded (A)

2. 用户绘制管线B
   → 撤销栈：添加 AddEntityCommand
   → 待保存列表：添加 ChangeAdded (B)

3. 用户保存
   → 数据库：插入 A 和 B
   → 待保存列表：清空
   → 撤销栈：保持不变（仍可撤销）

4. 用户撤销（Ctrl+Z）
   → 撤销栈：撤销最后一个 AddEntityCommand (B)
   → 场景：B 从地图消失
   → 待保存列表：添加 ChangeDeleted (B)
   → 数据库：B 仍在数据库中

5. 用户再次保存
   → 数据库：删除 B
   → 待保存列表：清空
```

### 示例2：删除已保存的实体

```
1. 用户删除已保存的管线C
   → 撤销栈：添加 DeleteEntityCommand
   → 场景：C 从地图消失
   → 待保存列表：添加 ChangeDeleted (C)

2. 用户撤销删除（Ctrl+Z）
   → 撤销栈：撤销 DeleteEntityCommand
   → 场景：C 重新显示
   → 待保存列表：移除 ChangeDeleted (C)
   → 数据库：C 仍在数据库中

3. 用户保存
   → 数据库：无变更（因为 ChangeDeleted 已移除）
   → 待保存列表：清空
```

---

## 📊 数据结构说明

### 待保存变更类型

```cpp
enum ChangeType {
    ChangeAdded,      // 新增
    ChangeModified,   // 修改
    ChangeDeleted     // 删除
};

struct PendingChange {
    ChangeType type;              // 变更类型
    QString entityType;          // "pipeline" 或 "facility"
    QVariant data;               // Pipeline 或 Facility 对象
    int originalId;              // 原始数据库ID（用于修改和删除）
    QGraphicsItem *graphicsItem; // 关联的图形项
};
```

### 命令类层次

```
QUndoCommand (Qt基类)
├── AddEntityCommand        // 添加实体
├── DeleteEntityCommand     // 删除实体
├── ChangeStyleCommand      // 修改样式
├── MoveEntityCommand       // 移动实体
└── ChangePropertyCommand   // 修改属性
```

---

## 🎯 关键实现函数

### 撤销/重做按钮处理

- **撤销按钮**：`handleUndoButtonClicked()` (第2072行)
  - 执行撤销操作
  - 调用 `handleUndoForPendingChanges()` 更新待保存列表
  - 刷新标注层

- **重做按钮**：`handleRedoButtonClicked()` (第2371行)
  - 执行重做操作
  - 调用 `handleRedoForPendingChanges()` 更新待保存列表
  - 刷新标注层

### 待保存列表处理

- **撤销处理**：`handleUndoForPendingChanges()` (第2095行)
  - 检查场景中的图形项
  - 更新 `m_pendingChanges` 列表
  - 处理已保存和未保存的实体

- **重做处理**：`handleRedoForPendingChanges()` (第2211行)
  - 检查场景中的图形项
  - 更新 `m_pendingChanges` 列表
  - 处理已保存和未保存的实体

---

## ⚠️ 重要注意事项

1. **撤销栈限制**
   - 最多支持50步撤销
   - 超过50步的操作无法撤销

2. **保存后撤销**
   - 保存后撤销栈不会清空
   - 可以撤销保存前的操作
   - 撤销已保存的实体会创建删除记录

3. **数据一致性**
   - 撤销/重做会同步更新待保存变更列表
   - 确保界面状态与待保存列表一致

4. **刷新数据**
   - 刷新数据时会清空撤销栈（根据文档说明）
   - 刷新会重新加载数据库数据

---

## 📝 总结

### 保存前的撤销/重做
- ✅ 撤销未保存的实体：从待保存列表移除
- ✅ 撤销已保存的实体：添加删除记录到待保存列表
- ✅ 重做未保存的实体：添加到待保存列表
- ✅ 重做已保存的实体：从待保存列表移除删除记录

### 保存后的撤销/重做
- ✅ 撤销栈不会清空，仍可撤销保存前的操作
- ✅ 撤销已保存的实体会创建删除记录
- ✅ 重做会移除删除记录，恢复实体显示
- ✅ 下次保存时会同步数据库状态

---

**最后更新**: 2025-01-27

